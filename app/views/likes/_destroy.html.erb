<li>
<% #byebug %>
  <%= form_for [@book, @book.likes.find_by(book_id: @book.id)], :method => :delete, :remote => true do |f| %>
    <%= f.hidden_field :user_id, :value=>current_user.id %>
    <%= f.hidden_field :book_id, :value=>@book.id %>
    <%= f.submit t(".unlike"), :class => "btn-1 sm shadow-0" %>
  <% end %>
</li>

